@baseUrl = http://localhost:8888/api/v1
@contentType = application/json

### 1. SIGN IN (as admin or authorized user)
# @name login
POST {{baseUrl}}/public/signin
Content-Type: {{contentType}}

{
  "email": "alex.johnson@example.com",
  "password": "123456"
}

### 2. EXTRACT TOKEN
@authToken = {{login.response.body.token}}
@userIdToDelete = 5

### 3. GET All Users
GET {{baseUrl}}/users
Authorization: Bearer {{authToken}}
Accept: {{contentType}}

### 4. GET User by ID (ID=1)
GET {{baseUrl}}/users/1
Authorization: Bearer {{authToken}}
Accept: {{contentType}}

### 5. CREATE User (Trainer)
# ⚠️ If this endpoint is PUBLIC (like signup), remove Authorization
POST {{baseUrl}}/users
Content-Type: {{contentType}}
Authorization: Bearer {{authToken}}  // ← Remove if public

{
  "role": "TRAINER",
  "firstName": "New",
  "lastName": "Trainer",
  "email": "new.trainer@example.com"
}

### 6. CREATE User (Trainee) - Duplicate Email (Expected 400)
# Same note: remove Authorization if public
POST {{baseUrl}}/users
Content-Type: {{contentType}}
Authorization: Bearer {{authToken}}  // ← Remove if public

{
  "role": "TRAINEE",
  "firstName": "Duplicate",
  "lastName": "Email",
  "email": "sarah.williams@example.com"
}

### 7. DELETE User (ID=5)
DELETE {{baseUrl}}/users/{{userIdToDelete}}
Authorization: Bearer {{authToken}}